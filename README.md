# wiki

Alex's persistent LLM-maintained knowledge base (Karpathy "LLM Wiki" pattern).
Start at `SCHEMA.md`, then `wiki/index.md`. Sync with `bin/sync.sh`. Secrets never enter this repo (`.secrets/` is gitignored).
