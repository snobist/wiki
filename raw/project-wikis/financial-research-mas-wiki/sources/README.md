# Sources

Raw inputs cited by wiki entries. Session-derived query outputs and scripts live in
session scratchpads and are summarized in entries; durable raw material goes here.

- 2026-07 diagnosis scripts (earnings_diag.py, regrade.py, calib_backtest.py) —
  walk-forward backtests and the beat-vs-reaction study; results compiled into
  [[wiki/earnings-scoring]] and [[wiki/signal-quality]]. Scripts were scratchpad-only;
  re-derive from the entries if needed.
- Live DB (`/data/mas.db` on the OCI box) is the ground truth for all metrics —
  entries record dated snapshots, not live state.
