# raw/ — immutable sources

Drop things here for ingestion; never edit in place. Suggested subfolders:
`codex-memories/` (imported Codex memory + access notes, 2026-08-31 bundle), `project-wikis/` (frozen
per-project wikis from ~/Documents/ClaudeProjects), `meetings/` (transcripts / notes), `docs/`, `assets/`.
